import 'package:flutter/material.dart';

class Chat extends StatelessWidget {

  final String chatId;

  const Chat(this.chatId, {Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
